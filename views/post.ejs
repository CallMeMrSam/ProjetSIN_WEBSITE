<%- include('helpers/header', { user, b: false }); %>
  <section class="hero is-fullheight">
    <div class="hero-body">
      <div class="container has-background-white p-4 radius">
        <form action="POST" method="POST">

          <div class="field">
            <label class="label">Titre</label>
            <div class="control">
              <input id="title" name="title" class="input" type="text" placeholder="Titre" required>
            </div>
          </div>
          <div class="field">
            <label class="label">Description</label>
            <div class="control">
              <input id="description" name="description" class="input" type="text" placeholder="Descritpion">
            </div>
          </div>
                    

          <div class="field">
            <label class="label">Article</label>
            <div class="control">
              <textarea id="content" name="content" class="textarea" placeholder="Contenu de l'article" required></textarea>
            </div>
          </div>

					<div class="control">
						<button type="submit" class="button is-primary">Envoyer</button>
					</div>
        </form>
      </div>
    </div>
  </section>
<%- include('helpers/footer.ejs') %>